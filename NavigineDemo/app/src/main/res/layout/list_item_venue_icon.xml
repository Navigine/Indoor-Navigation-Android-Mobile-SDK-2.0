<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="@dimen/venue_icon_group_height"
    android:layout_height="@dimen/venue_icon_group_height"
    android:gravity="center"
    android:orientation="vertical"
    android:weightSum="3">

    <ImageView
        android:id="@+id/li_venue_icon"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:clickable="true"
        android:focusable="true"
        tools:src="@drawable/ic_venue_atm_banks" />

    <Space
        android:layout_width="match_parent"
        android:layout_height="8dp" />

    <TextView
        android:id="@+id/li_venue_title"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="5dp"
        android:layout_weight="1"
        android:ellipsize="end"
        android:gravity="center|top"
        android:lineSpacingExtra="-4dp"
        android:maxLines="2"
        android:textSize="10sp"
        android:textStyle="bold"
        tools:text="@string/venue_banking" />

</LinearLayout>